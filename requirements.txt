# Core dependencies for emotion detection model
transformers>=4.30.0
torch>=2.0.0

# Data processing
pandas>=2.0.0

# Evaluation metrics
scikit-learn>=1.3.0

# RASA SDK for custom actions
rasa-sdk>=3.0.0

# FastAPI and web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.0.0

# HTTP requests for LLM API (httpx is already listed above)

# Translation uses Groq LLM (no extra deps)

# Firebase Admin SDK (optional - only needed if using Firebase features)
firebase-admin>=6.0.0

# For downloading models from Google Drive
requests>=2.31.0

